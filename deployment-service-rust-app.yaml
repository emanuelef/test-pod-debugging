apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-rust-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-rust-app
  template:
    metadata:
      labels:
        app: test-rust-app
    spec:
      containers:
        - name: test-rust-app
          image: ghcr.io/emanuelef/test-rust-app:latest
          ports:
            - containerPort: 8080
          env:
            - name: API_HOST
              value: python-app-service.default.svc.cluster.local

---
apiVersion: v1
kind: Service
metadata:
  name: test-rust-app-service
spec:
  selector:
    app: test-rust-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
